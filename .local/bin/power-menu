#!/bin/sh
# Power menu

POWER_OP="$(echo -e "Logout\nShutdown\nReboot" | rofi -dmenu -i)"
[[ $POWER_OP = "Shutdown" ]] && systemctl poweroff
[[ $POWER_OP = "Reboot" ]] && systemctl reboot
[[ $POWER_OP = "Logout" ]] && niri msg action quit -s

